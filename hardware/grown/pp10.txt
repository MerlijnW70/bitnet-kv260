# The partial product x(k-10) AND y_10 of a 16-bit serial multiplier, laid by construction
# in board/research/ternary/ppplan.py: x on row 0 and y on row 1 a bit a step, least significant
# first, a one-hot step chain and an x delay chain of 10 state rows each, a latch of y_10 set
# by the pulse of step 10, and the product on the lane row 61 at step k, 16 steps of word
# and 16 of hold. 40 gates, whole on the host model over corners and random operand pairs.
inputs 32
outputs 32
rows 64
columns 10
steps 16
state 59
hold 16
fold 1 below
gates sum maj not up down leap hop
colony 4096
renew 32
sample 256
confirm 65536
packed
lean wires
seed 4343
serial pp 10
